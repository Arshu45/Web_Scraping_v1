# ── Core Scraping ──────────────────────────────────
scrapy

# ── Database ───────────────────────────────────────
sqlalchemy
psycopg2-binary          # PostgreSQL driver
alembic                  # DB schema migrations

# ── Orchestration ──────────────────────────────────
prefect                  # Pipeline scheduling & monitoring

# ── Image-Based Promo Scraper ──────────────────────
playwright               # Headless browser automation
google-genai             # Gemini 2.0 Flash Vision API (new SDK)
pillow                   # Image resizing before API upload
tenacity                 # Exponential backoff for API retries

# ── Dashboard (Streamlit) ──────────────────────────
streamlit
pandas
plotly

# ── Utilities ──────────────────────────────────────
python-dotenv            # Load .env configuration
httpx                    # HTTP client for downloading images

